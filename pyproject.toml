[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "escape-room"
version = "0.1.0"
description = "A text-based escape room game built with Python."
requires-python = ">=3.14"

[tool.setuptools.packages.find]
where = ["src"]